# Production profile — all values must come from environment variables.
# No local file imports. Set SPRING_PROFILES_ACTIVE=prod on the server.
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# UPLOAD_BASE_URL must be set explicitly in production (no localhost fallback).
upload.base-url=${UPLOAD_BASE_URL}
